
<div class="span9 content">
  <%= create_guide("Machine Categories", 
        "Office: <b>#{current_office.name}</b>" + "<br />"    +  
        "Total Machine Categories: <b>#{current_office.machine_categories.count}</b> "      ).html_safe %>
  
  <%#= create_breadcrumb(@breadcrumbs).html_safe %>
  <% if flash[:notice] %>
    <div class="alert alert-success">
      <a data-dismiss="alert" class="close">×</a>
      <strong>Well done!</strong> <%= flash[:notice].html_safe %>
    </div>
  <% end %>
  
  <% if flash[:error] %>
    <div class="alert alert-error">
      <a data-dismiss="alert" class="close">×</a>
      <strong>Create Failure!</strong> Please fill in all fields.
    </div>
  <% end %>
  
  
  <div class="row-fluid">
    <div class="span12">
      
      <h2>New Machine Category</h2><br />

      <%= simple_form_for(@new_machine_category, :html => {:class => 'form-horizontal prevent_double_submit' }) do |f| %>
        
        <%= f.input :name , :input_html => {:class => "input-xlarge span5"}, 
         :label => "Kategori Mesin" %>


        <div class="form-actions">
          <%= f.button :submit , :class => "btn btn-primary", :text=> "Create Machine Category" %>
        </div>
      <% end %>
      
      
     
      
    </div> 
      
      
      
    
  </div><!--/row-->
   <hr />
  <div class="row-fluid">
    <div id="data_list">
      <h2>Total Machine Categories in  <%= current_office.name %>: <%= current_office.machine_categories.count %> </h2>
      
      
      <table cellpadding="0" cellspacing="0" border="0" 
            class="table table-striped table-bordered" id="example">
        <thead>
          <tr>
            <th>Kategori</th>
            <th>Total Mesin</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% current_office.machine_categories.each do |machine_category| %>
            <tr>
              <td><%= machine_category.name  %></td>
              <td><%= machine_category.machines.count %></td>
              <td><button href="#" class="btn">Delete</button></td>
            </tr>
          <% end %>

        </tbody>
      </table>
    </div><!-- end of #data_list -->
  </div>

</div><!--/span-->















